# ── Smart Lookup — fuzzy column name matching (Layer 2) ─────────────────────
# RapidFuzz: token_sort_ratio / ratio for src↔tgt column name similarity
RapidFuzz>=3.0

# ── PDF import ───────────────────────────────────────────────────────────────
# pdfplumber: parse PDF tables for import (V2.5+)
pdfplumber>=0.10

# ── DuckDB Arrow IPC transport ───────────────────────────────────────────────
# pyarrow: serialize DocType rows as Arrow IPC binary for bulk_fetch_for_duckdb.
# 3-5x smaller than JSON; native DuckDB Arrow reader on client.
# Falls back to JSON rows if unavailable (backward-compatible).
pyarrow>=14.0
